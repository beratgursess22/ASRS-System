# Raspberry Pi Python Bağımlılıkları
# Kurulum: pip install -r requirements.txt

# RFID okuyucu (RC522 için)
mfrc522>=0.0.7

# USB seri haberleşme (Arduino Mega)
pyserial>=3.5

# GPIO erişimi (ör. geliştirme amaçlı test için)
RPi.GPIO>=0.7.1
