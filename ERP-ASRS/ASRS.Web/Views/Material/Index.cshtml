@model IEnumerable<ASRS.Core.DTOs.MaterialListDto>
@{
    ViewData["Title"] = "Malzeme Yönetimi";
}

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" asp-append-version="true" />
}

<div class="page-header">
    <h1 class="page-title">Malzeme Yönetimi</h1>
</div>

@if (ViewBag.Error != null)
{
    <div class="alert-box alert-error">@ViewBag.Error</div>
}

@if (User.IsInRole("Yönetici") || User.IsInRole("Depo"))
{
    <div class="card" style="margin-bottom: 1.5rem;">
        <div class="card-header">
            <h2 class="card-title">Yeni Malzeme Ekle</h2>
        </div>
        <form method="post" asp-action="Create">
            <div class="form-row">
                <div class="form-group">
                    <label>Malzeme Kodu</label>
                    <input type="text" name="Code" class="form-input" required />
                </div>
                <div class="form-group">
                    <label>Malzeme Adı</label>
                    <input type="text" name="Name" class="form-input" required />
                </div>
                <div class="form-group">
                    <label>Birim</label>
                    <input type="text" name="Unit" class="form-input" placeholder="adet, kg, lt..." required />
                </div>
                <div class="form-group">
                    <label>Stok Miktarı</label>
                    <input type="number" name="StockQuantity" class="form-input" value="0" min="0" />
                </div>
                <div class="form-group">
                    <label>Min. Stok Seviyesi</label>
                    <input type="number" name="MinStockLevel" class="form-input" value="0" min="0" />
                </div>
                <div class="form-group">
                    <label>Varsayilan Birim Fiyat</label>
                    <input type="number" name="DefaultUnitPrice" class="form-input" value="0" min="0" step="0.01" />
                </div>
                <div class="form-group">
                    <label>Varsayilan Para Birimi</label>
                    <input type="text" name="DefaultCurrency" class="form-input" value="TRY" maxlength="10" />
                </div>
                <div class="form-group">
                    <label>Açıklama</label>
                    <input type="text" name="Description" class="form-input" />
                </div>
            </div>
            <button type="submit" class="btn-primary">Malzeme Ekle</button>
        </form>
    </div>
}

<div class="card">
    <div class="card-header">
        <h2 class="card-title">
            @if (!string.IsNullOrEmpty(ViewBag.Search))
            {
                <span>@Model.Count() sonuç — "@ViewBag.Search"</span>
            }
            else
            {
                <span>@Model.Count() malzeme</span>
            }
        </h2>
        <form method="get" asp-action="Index" style="display:flex; gap:0.5rem;">
            <input type="text" name="search" class="form-input" placeholder="Ara..." value="@ViewBag.Search" style="width:200px;" />
            <button type="submit" class="btn-primary">Ara</button>
            @if (!string.IsNullOrEmpty(ViewBag.Search))
            {
                <a asp-action="Index" class="btn-edit">Temizle</a>
            }
        </form>
    </div>
    <table class="data-table">
        <thead>
            <tr>
                <th>Kod</th>
                <th>Malzeme Adı</th>
                <th>Birim</th>
                <th>Stok</th>
                <th>Min. Stok</th>
                <th>Varsayilan Fiyat</th>
                <th>Açıklama</th>
                <th>Durum</th>
                @if (User.IsInRole("Yönetici") || User.IsInRole("Depo"))
                {
                    <th>İşlem</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var m in Model)
            {
                <tr>
                    <td><span style="font-family:monospace; color:var(--accent);">@m.Code</span></td>
                    <td>@m.Name</td>
                    <td>@m.Unit</td>
                    <td>
                        @if (m.StockQuantity <= m.MinStockLevel)
                        {
                            <span style="color:#f87171; font-weight:600;">@m.StockQuantity</span>
                        }
                        else
                        {
                            <span>@m.StockQuantity</span>
                        }
                    </td>
                    <td>@m.MinStockLevel</td>
                    <td>@m.DefaultUnitPrice.ToString("N2") @m.DefaultCurrency</td>
                    <td>@(m.Description ?? "-")</td>
                    <td>
                        @if (m.IsActive)
                        {
                            <span class="badge badge-active">Aktif</span>
                        }
                        else
                        {
                            <span class="badge badge-inactive">Pasif</span>
                        }
                    </td>
                    @if (User.IsInRole("Yönetici") || User.IsInRole("Depo"))
                    {
                        <td style="display:flex; gap:0.5rem;">
                            <a asp-action="Edit" asp-route-id="@m.Id" class="btn-edit">Düzenle</a>
                            <form method="post" asp-action="Delete" asp-route-id="@m.Id" onsubmit="return confirm('@m.Name silinecek. Emin misiniz?')">
                                <button type="submit" class="btn-delete">Sil</button>
                            </form>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>
